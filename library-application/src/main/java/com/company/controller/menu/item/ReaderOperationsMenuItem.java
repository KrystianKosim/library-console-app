package com.company.controller.menu.item;

import com.company.controller.menu.utils.MenuAction;
import com.company.controller.menu.utils.MenuItem;

@MenuItem(code = "READER OPERATIONS", description = "Operations on READER", parent = {"ADMIN"})
public class ReaderOperationsMenuItem extends MenuAction {

    @Override
    public void action() {
    }
}
